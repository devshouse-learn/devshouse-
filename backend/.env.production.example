# Variables de entorno para producción (Elastic Beanstalk)
# NO commitear este archivo con valores reales

# Aplicación
NODE_ENV=production
PORT=8080
API_VERSION=v1

# CORS - Actualizar con la URL real del frontend
CORS_ORIGIN=http://tu-bucket.s3-website-region.amazonaws.com

# Base de datos (si usas MongoDB)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/devshouse

# Autenticación JWT (generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# JWT_SECRET=tu-secret-muy-seguro-aqui
# JWT_EXPIRES_IN=7d

# Email (si usas servicios de email)
# EMAIL_SERVICE=gmail
# EMAIL_USER=tu-email@gmail.com
# EMAIL_PASSWORD=tu-password

# Logging
LOG_LEVEL=info

# Seguridad
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
