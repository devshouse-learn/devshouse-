import './Hero.css';

const Hero = () => (
  <section className="hero">
    <div className="hero-content">
      <p className="hero-subtitle">Conectando educación, emprendimiento y oportunidades laborales <span className="emoji">✨</span></p>
    </div>
  </section>
);

export default Hero;
